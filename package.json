{
  "name": "vamet",
  "description": "vAMET - Virtual Aeromet panel for virtual air traffic controllers.",
  "version": "5.0.0",
  "license": "ISC",
  "author": "DookthorAndriss",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "start": "npm run build:all && node dist/index.js",
    "build:exe": "npx esbuild index.js --bundle --platform=node --format=cjs --outfile=dist/server.cjs && pkg dist/server.cjs --targets node18-win-x64 --output vAMET.exe",
    "build:server": "tsc",
    "build:web": "tsc -p ./web/tsconfig.json",
    "build:all": "npm run build:server && npm run build:web"
  },
  "dependencies": {
    "axios": "^1.16.0",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "readline": "^1.3.0",
    "socket.io": "^4.8.3",
    "typescript": "^6.0.3"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "pkg": "^5.8.1"
  }
}
